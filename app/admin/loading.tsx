export default function AdminLoading() {
  return (
    <main className="grid min-h-[100dvh] place-items-center bg-[var(--color-canvas)] px-5 text-[var(--color-ink)]">
      <p className="font-mono text-xs font-black tracking-[.2em] text-[var(--color-ink-muted)] uppercase">
        Cargando portal privado…
      </p>
    </main>
  );
}
